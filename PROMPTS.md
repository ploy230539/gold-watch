# prompt ใหม่ของ scheduled task ทั้ง 3 ตัว

URL ใหม่ที่ใช้แทน Artifact เดิมทุกจุด (อีเมล ปุ่ม CTA แชท push):

```
https://ploy230539.github.io/gold-watch/
```

Artifact เดิม `751af5ad-2435-4077-8879-2bd35a1ccaa8` **เลิกใช้** ไม่ต้อง publish ไปที่นั่นอีก

---

## ก่อนแก้ prompt — ต้องต่อ GitHub connector บน claude.ai ก่อน

3 tasks รันบน claude.ai ซึ่ง **ไม่มี shell** จึงรัน `gw.mjs` เองไม่ได้
สะพานคือ: task เขียนแต่ **ไฟล์ JSON** ลง repo ผ่าน GitHub connector →
GitHub Action เป็นคนเติม template, คำนวณ ladder, validate placeholder และ commit `docs/index.html` ให้

ผลพลอยได้: task ไม่ต้องประกอบ HTML เองอีกเลย ซึ่งเป็นจุดที่พังบ่อยที่สุด

- repo: `ploy230539/gold-watch` (public)
- ไฟล์ที่ task เขียน: `data/payload-latest.json` (และ `data/state.json` เฉพาะรอบเฝ้าราคาที่ส่งจริง)
- ไฟล์ที่ task **ห้ามแตะ**: `docs/index.html`, `data/log.json` — Action เป็นคนเขียน
- อ่านไฟล์ปัจจุบันได้โดยไม่ต้อง auth เพราะ repo เป็น public:
  `https://raw.githubusercontent.com/ploy230539/gold-watch/main/data/state.json`
  `https://raw.githubusercontent.com/ploy230539/gold-watch/main/data/log.json`

โครง payload เต็มพร้อมคำอธิบายทุกฟิลด์อยู่ที่ `payload.example.json` ใน repo

---

## 1. สรุปทองเช้า 8 โมง — `trig_01BhMndhqMhs1AYomdQnpCzS`

cron เดิม `0 1 * * 1-6` ไม่ต้องแก้

> ทำสรุปทองเช้าให้พลอย ตามสกิล gold-watch
>
> **1. ค้นราคาจริง** — Gold Spot, ทองแท่งไทยรับซื้อ/ขายออก, ทองรูปพรรณขายออก, USD/THB
> จาก Trading Economics, ข่าวไทยที่อ้างสมาคมค้าทองคำ, FXStreet
> ห้ามเดาตัวเลขเด็ดขาด ทุกตัวเลขต้องมีเวลาที่เก็บกำกับ
> ถ้าสองแหล่งให้ Spot ต่างกันเกิน $20 ให้รายงานเป็นช่วงและบอกว่าเก็บคนละเวลา
> ฟิลด์ไหนดึงไม่ได้จริงๆ ให้ใส่ `null` หน้าเว็บจะขึ้น `—` ให้เอง — ห้ามเดามาใส่แทน
>
> **2. เขียนสรุปในแชท** ตามโครง 7 ข้อในสกิล โทนแชทใช้ มึง/กู ได้ตามปกติ
>
> **3. เขียน payload ขึ้น GitHub** — ใช้ GitHub connector เขียนทับไฟล์
> `data/payload-latest.json` ใน repo `ploy230539/gold-watch` branch `main`
> โครงไฟล์ดูจาก `payload.example.json` ใน repo เดียวกัน ต้องมีครบทั้ง
> `stamp, lede_h1, lede_p, spot, thb_bar, thb_orn, fx, levels, rsi, read_text,
> invalid_text, drivers, calendar, sources, call, log`
> `call` เป็นหนึ่งใน `buy` / `hold` / `wait`
> `log` ใส่ `date_display, date_iso, actual_due_iso, actual_due_display`
> โดย due = 3 วันทำการถัดไป
>
> **ห้ามเขียน `docs/index.html` และ `data/log.json` เอง** — GitHub Action จะเติม template
> คำนวณตำแหน่งแนวรับแนวต้าน เติมผลจริงย้อนหลังในสมุดบันทึก เพิ่มแถวใหม่
> แล้ว commit ให้เองภายใน 1–2 นาที
> ถ้า Action ล้มเหลว (ดูที่ https://github.com/ploy230539/gold-watch/actions)
> ให้บอกในแชทว่าหน้าเว็บยังไม่อัปเดต **ห้ามแก้ HTML ด้วยมือ**
>
> **4. สมุดบันทึกผลงาน** — ไม่ต้องไปค้นราคาย้อนหลังมาเติมผลจริงเองแล้ว
> Action เติมให้อัตโนมัติจากราคาที่ส่งมาในรอบนี้ (วัดแบบ close-to-close ขยับไม่ถึง ±0.5% นับเสมอ)
> ถ้าอยากอ้างถึงผลย้อนหลังในแชทหรืออีเมล ให้อ่านจาก
> https://raw.githubusercontent.com/ploy230539/gold-watch/main/data/log.json
> **ยังไม่ถึง 30 แถวที่มีผลจริง ห้ามอ้าง % ความแม่นเด็ดขาด** และเขียนครั้งที่พลาดให้ชัดเท่าครั้งที่ถูก
>
> **5. ส่งอีเมลสรุปเช้า** ถึง iminiwindy@gmail.com และ pongkasame.oil@gmail.com
> โทนไทยสุภาพเป็นกลางแบบนักวิเคราะห์ที่เป็นกันเอง **ห้ามใช้ มึง/กู เด็ดขาด** เพราะมีคนอื่นอ่าน
> ปุ่ม/ลิงก์ "ดู dashboard เต็ม" ชี้ไปที่ https://ploy230539.github.io/gold-watch/
> บอกในอีเมลด้วยว่าลิงก์นี้เปิดได้เลยไม่ต้องล็อกอิน
> ปิดท้ายด้วย disclaimer ว่าไม่ใช่คำแนะนำการลงทุน
>
> **6. เช็คเกณฑ์ push** ตามสกิล ถ้าเข้าเกณฑ์ค่อยยิง ไม่เข้าเกณฑ์ไม่ต้องยิง

---

## 2. เฝ้าราคาทอง — `trig_01SnAhscWctZqTgUuEuBrreZ`

cron เดิม `0 2,5,8,11 * * 1-5` ไม่ต้องแก้

> เฝ้าราคาทองให้พลอย ตามสกิล gold-watch
>
> **1. อ่านราคาที่แจ้งไปครั้งล่าสุด** จาก
> https://raw.githubusercontent.com/ploy230539/gold-watch/main/data/state.json
> ใน `last_alert` มี `thb_sell` (ทองแท่งขายออก) และ `xau` (Spot ปัดเต็ม) — นี่คือค่าอ้างอิง
> ถ้าดึงไฟล์ไม่ได้จริงๆ ให้ fallback ไปอ่านรหัสท้าย subject อีเมลแบบเดิม
> (Gmail search `in:sent subject:"ทองขยับ" newer_than:2d` รูปแบบ `[TH <ทองแท่งขายออก> | XAU <spot>]`)
> แล้วบอกในแชทว่า state file มีปัญหา
>
> **2. เช็คราคาปัจจุบันจริง** จากเว็บ ห้ามเดา
>
> **3. ตัดสินใจตามเกณฑ์นี้ (ห้ามเปลี่ยน)**
> - ทองไทยขยับ **≥ 150 บาท** จากค่าอ้างอิงข้างบน (สะสมได้ ไม่ใช่เทียบเมื่อวาน) → แจ้ง
> - หรือ Spot ขยับ **≥ 1.5%** จากค่าอ้างอิง → แจ้ง
> - หรือมีข่าวใหญ่ระดับเขย่าตลาด → แจ้ง
> - **ไม่เข้าเกณฑ์ = เงียบสนิท ไม่ส่งอะไรเลย ไม่ต้องเขียนอะไรในแชทด้วย**
>   การเตือนที่ไม่จำเป็นทำให้พลอยเลิกอ่านการเตือนที่จำเป็น
>
> ถ้าเข้าเกณฑ์ เลือกช่องทางตามความแรง:
> - ทองไทย **≥ 300 บาท** หรือ Spot **≥ 1.5%** หรือข่าวใหญ่ → อีเมล + push + แชท
> - ขยับ **150–300 บาท** → อีเมล + แชท เท่านั้น **ไม่ต้องเด้ง push**
>
> **4. ถ้าส่ง** — อีเมลถึง iminiwindy@gmail.com และ pongkasame.oil@gmail.com
> โทนไทยสุภาพเป็นกลาง **ห้าม มึง/กู** (แชทกับ push ใช้ได้ตามปกติ)
> subject ลงท้ายด้วยรหัสเดิม `[TH <ทองแท่งขายออก> | XAU <spot ปัดเต็ม>]` เหมือนเดิม
> เพื่อให้คนอ่านเห็นว่าเทียบกับราคาไหน และใช้กู้ state ได้ถ้าไฟล์มีปัญหา
> ลิงก์ dashboard ในอีเมล/แชท/push ใช้ https://ploy230539.github.io/gold-watch/
>
> **5. หลังส่งเสร็จเท่านั้น** ใช้ GitHub connector เขียนทับ `data/state.json`
> ใน repo `ploy230539/gold-watch` branch `main` ให้ `last_alert` เป็น
> `{"ts_iso": "<เวลาที่ส่ง ISO>", "thb_sell": <ราคาที่เพิ่งแจ้ง>, "xau": <spot ที่เพิ่งแจ้ง>, "note": "<สรุปสั้นๆ ว่าทำไมถึงแจ้ง>"}`
> และ append entry เดิมเข้า `history` (เก็บไว้ไม่เกิน 100 รายการ)
> **ถ้าไม่ได้ส่ง ห้ามแตะ state.json** ไม่งั้นการนับแบบสะสมจะเพี้ยน
>
> รอบนี้ห้ามแตะ `data/payload-latest.json`, `data/log.json`, `docs/index.html`

---

## 3. ทบทวนตัวเองต้นเดือน — `trig_0147WdSRtjXbRaaHyR8DAmLH`

cron เดิม `0 2 1 * *` ไม่ต้องแก้

> ทบทวนความแม่นของมุมมองทองย้อนหลัง ตามสกิล gold-watch ข้อ 9
>
> **1. อ่านสมุดบันทึกผลงาน** จาก
> https://raw.githubusercontent.com/ploy230539/gold-watch/main/data/log.json
> (ย้ายมาจาก Drive แล้ว ไฟล์บน Drive เป็นของเก่าไม่ได้อัปเดตแล้ว)
> `actual_result` ที่ขึ้นต้นด้วย `✓` คือตรง `✗` คือพลาด `=` คือเสมอ
> แถวที่ `actual_result` เป็น `null` คือยังไม่ถึงกำหนดวัดผล **ห้ามนับ**
>
> **2. วิเคราะห์ว่าคิดผิดตรงไหนซ้ำๆ** — สนใจ pattern ของความผิดพลาดเป็นหลัก
> เช่น เชียร์ "รอ" ตอนตลาดขาขึ้นบ่อยไป หรือตั้งแนวรับแคบเกินจริง
> - **นับเฉพาะแถวที่ `actual_result` ไม่ใช่ null**
> - **ต่ำกว่า 30 แถวห้ามอ้าง % ความแม่นเด็ดขาด** ให้บอกแค่ว่ายังเก็บข้อมูลอยู่
> - ถ้าช่วงที่เก็บเป็นตลาดเทรนด์เดียว ให้บอกตรงๆ ว่าสถิตินี้ยังใช้ตัดสินอะไรไม่ได้
> - เขียนครั้งที่พลาดให้เด่นกว่าครั้งที่ถูก
>
> **3. เสนอแก้สกิล gold-watch** — เสนอเฉยๆ **ห้ามแก้เอง**
>
> ผลลัพธ์เขียนในแชท ไม่ต้อง publish ขึ้นเว็บ ไม่ต้องส่งอีเมล

---

## ถ้าไม่อยากต่อ GitHub connector

ทางเลือกสำรอง: ย้าย 3 tasks มารันเป็น cron ของ Claude Code บนเครื่องพลอยแทน
เครื่องนี้มีทั้ง Gmail/Drive/push connector และมี shell จึงเรียก `node gw.mjs morning`
กับ `node gw.mjs publish` ได้ตรงๆ ไม่ต้องมี Action เป็นสะพาน
ข้อแลกเปลี่ยนคือ **เครื่องต้องเปิดอยู่ตอน 08:00 / 09:00 / 12:00 / 15:00 / 18:00 น.**
ถ้าเครื่องปิด รอบนั้นหายไปเลย
